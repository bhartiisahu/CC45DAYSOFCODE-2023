#include <iostream>
using namespace std;

int main() {
    int x,y,t;
    cin>>t;
    while(t--)
    {
        cin>>x>>y;
        if(10*y>=x)
        {
            cout<<"No"<<endl;
        }
        else
        {
            cout<<"Yes"<<endl;
        }
    }

	return 0;
}
