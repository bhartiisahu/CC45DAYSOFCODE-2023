#include <iostream>
using namespace std;

int main() {
	int t,n;
	cin>>t;
	while(t--)
	{
	    cin>>n;
	    if(n<16)
	    {
	        cout<<"Lower ";
	        if(n<11)
	        {
	            cout<<"Double"<<endl;
	        }
	        else
	        {
	            cout<<"Single"<<endl;
	        }
	    }
	    else
	    {
	        cout<<"Upper ";
	        if(n<26)
	        {
	            cout<<"Double"<<endl;
	        }
	        else
	        {
	            cout<<"Single"<<endl;
	        }
	    }
	  
	}
	return 0;
}
